package com.dragn0007.dffeasts.entity.animal;

/*

import com.dragn0007.dffeasts.DFFeastsMain;
import net.minecraft.client.model.EntityModel;
import net.minecraft.client.renderer.entity.EntityRendererProvider;
import net.minecraft.client.renderer.entity.MobRenderer;
import net.minecraft.resources.ResourceLocation;
import net.minecraftforge.api.distmarker.Dist;
import net.minecraftforge.api.distmarker.OnlyIn;
import net.minecraftforge.fml.common.Mod;


@Mod(DFFeastsMain.MODID)
@OnlyIn(Dist.CLIENT)
public class DuckRender extends MobRenderer<Duck, DuckModel<Duck>> {
    private static final ResourceLocation TEXTURE = new ResourceLocation("textures/entity/duck.png");

    public DuckRender(EntityRendererProvider.Context context) {
        super(context, new DuckModel<>(context.bakeLayer(DuckModel.LAYER_LOCATION)), 0.3F);
    }

    @Override
    public ResourceLocation getTextureLocation(Duck duck) {
        return TEXTURE;
    }
}

 */